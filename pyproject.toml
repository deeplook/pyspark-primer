[project]
name = "sparky"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipython>=9.13.0",
    "pyspark>=4.1.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
